{
  "name": "spotify-widget",
  "version": "1.0.0",
  "main": "dist/index.html",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && parcel build src/index.html",
    "clean": "rimraf dist/",
    "start": "yarn clean && parcel src/index.html"
  },
  "devDependencies": {
    "parcel": "^2.0.0-nightly.255",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "date-fns": "^2.13.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "styled-components": "^5.1.0",
    "use-persisted-state": "^0.3.0"
  }
}
